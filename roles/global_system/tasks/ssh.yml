- name: Global_System | SSH | Copy Config File
  ansible.builtin.template:
    src: "{{ global_system.config_path }}/sshd_config.j2"
    dest: /etc/ssh/sshd_config
    mode: '0644'
  notify: Restart SSH
