- name: Global_System | Packages | Gather Package Facts
  ansible.builtin.package_facts:
    manager: auto

- name: Global_System | Packages | Apt Installation
  ansible.builtin.apt:
    name: "{{ system_packages + (global_system_packages | default([])) }}"
    update_cache: true
  when: ansible_pkg_mgr == "apt"
