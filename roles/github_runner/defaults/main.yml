# username: null
# github_token: changeme
# github_org: changeme
# site: changeme
# runner_version: "2.321.0"
# runner_name: "{{ inventory_hostname }}"
# runner_labels: "self-hosted,linux,{{ site }}"
# runner_path: "/home/{{ username }}/actions-runner"


github_runner_username: null
# github_runner_token: changeme
github_runner_pat: changeme
github_runner_org: changeme
github_runner_site: changeme
github_runner_version: "2.335.1"
github_runner_name: "{{ inventory_hostname }}"
github_runner_labels: "self-hosted,linux,{{ site }}"
github_runner_path: "/home/{{ github_runner_username }}/actions-runner"